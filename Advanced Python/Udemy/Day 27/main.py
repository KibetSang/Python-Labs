import tkinter

window = tkinter.Tk()
window.title("Student Details" )
window.minsize(400, 400)

my_label = tkinter.Label(window, text="This Label", font = ("Arial", 16,))
my_label.pack(side = "left")











window.mainloop()